# Odido mobile detail copy + CTA fix v1

- Replaced the incorrect hard-coded `Budget Thuis` disclaimer on offer overview cards with a provider-aware fallback.
- Existing deal-specific `termsSummary` remains leading when present.
- On mobile/tablet widths the deal CTA no longer sticks over `Dealgegevens`; it flows after the specifications block. Desktop sticky CTA behaviour is untouched.
- No deal values, Awin links, banners or SEO URLs were changed.
