window.MPDeals={async load(){const r=await fetch('data/deals.json');return r.json()},render(items){const el=document.getElementById('dealGrid');if(!el)return;el.innerHTML=items.map(d=>`<article class="deal-card ${d.best?'is-best':''}" data-affiliate="true"><div class="deal-logo">${d.provider}</div><div class="deal-title">${d.title}</div><div class="deal-subtitle">${d.subtitle}</div><div class="deal-benefit">${d.benefits.map(b=>`<div class="benefit-line"><span class="benefit-icon">${b.includes('cashback')?'€':'🎁'}</span><span>${b}</span></div>`).join('')}</div><div class="score-row"><span>MeerPakScore</span><strong>⭐ ${d.score}</strong></div><button class="deal-button" type="button" data-provider="${d.provider}">Bekijk deal</button></article>`).join('')}};